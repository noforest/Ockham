"""Context representation stage: render(target_source, evidence) -> str.

The target is always rendered in full, and empty evidence gives a target-only pack, so
C0 means the same thing whichever representation it is crossed with.
"""
